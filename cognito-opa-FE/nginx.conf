server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optional: API proxy (only if needed)
    # location /api/ {
    #     proxy_pass http://backend-service;
    # }
}